<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.myweb.www.repository.BoardDAO">
	<select id="selectListBoard" resultType="com.myweb.www.domain.BoardVO">
		select a.bno, title, writer, reg_date, read_count from (
		select bno from board
		where isDel ="n"
		order by bno desc
		limit #{pageStart}, #{qty}) a left join board b
		on a.bno = b.bno
	</select>
	
	<select id="getTotalCount" resultType="int">
		select count(bno) from board where isDel="n"
	</select>
	
	<insert id="insertRegi">
		insert into board (title, context, writer)
		values (#{title}, #{context}, #{writer})
	</insert>
	
	<select id="selectBno" resultType="int">
		select max(bno) from board
	</select>
</mapper>